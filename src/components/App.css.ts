import {style} from '@vanilla-extract/css';

export const app = style({
  position: 'relative',
  display: 'flex',
  flexDirection: 'row',
  justifyContent: 'stretch',
  height: '100%',
  width: '100%'
});
